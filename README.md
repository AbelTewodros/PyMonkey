# PyMonkey
PyMonkey is a google extension that allows the user to run python using Pyodide. It also allows for manipulating, extracting and scraping web information without the hassle of setting up beautiful soup or selenium.
